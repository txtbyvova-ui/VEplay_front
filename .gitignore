# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.production

# Auth data (secrets / credentials — never commit)
.secret
users.json

# VEclassify bridge: staging area + batch state (transient, per-machine)
_incoming/
pending_batches.json

# Local dev tooling
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
